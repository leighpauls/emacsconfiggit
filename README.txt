To start using this config, place a softlink to:

File: ".emacs"
and
directory: ".emacs.d"

from your user home directory using ln -s ... and emacs will
 pick it up. You may need to delete any auto-generated
 configs from those locations.
